<?xml version="1.0" ?>
<psalm
  errorLevel="5"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns="https://getpsalm.org/schema/config"
  xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
  phpVersion="8.4"
  noCache="false"
  findUnusedPsalmSuppress="true"
  skipChecksOnUnresolvableIncludes="true"
  allowStringToStandInForClass="true"
  memoizeMethodCallResults="true"
  hoistConstants="true"
  addParamTypehint="false"
  checkForThrowsDocblock="false"
  checkForThrowsInGlobalScope="false"
  sealAllMethods="false"
  sealAllProperties="false"
>
  <projectFiles>
    <directory name="src" />
    <directory name="examples" />
    <directory name="config" />
    <directory name="tests" />
    <ignoreFiles>
      <directory name="vendor" />
      <directory name="src/Laravel" />
    </ignoreFiles>
  </projectFiles>

  <plugins>
    <pluginClass class="Psalm\PhpUnitPlugin\Plugin" />
  </plugins>

  <issueHandlers>
    <!-- Laravel function compatibility -->
    <UndefinedFunction>
      <errorLevel type="suppress">
        <referencedFunction name="config" />
        <referencedFunction name="app" />
        <referencedFunction name="now" />
        <referencedFunction name="config_path" />
        <referencedFunction name="env" />
      </errorLevel>
    </UndefinedFunction>

    <!-- Complex return type issues in GdprProcessor -->
    <InvalidReturnType>
      <errorLevel type="suppress">
        <file name="src/GdprProcessor.php" />
      </errorLevel>
    </InvalidReturnType>

    <!--
      Each suppression below was verified load-bearing: flipping it to "error" produces
      the issue count noted. Suppressions that produced zero issues at errorLevel 5
      (MissingOverrideAttribute, Mixed*, Missing{Return,Param,Property}Type,
      MismatchingDocblockReturnType, PropertyNotSetInConstructor, PossiblyUnusedProperty,
      PossiblyInvalidCast) were removed as dead config. Tightening errorLevel below 5 may
      surface those again — re-add them with a fresh justification if so.
    -->

    <!-- Class finalization - suppress to avoid API breaking changes (3 issues) -->
    <ClassMustBeFinal errorLevel="suppress" />

    <!-- Prevent Psalm from adding complex nested return types (50 + 50 issues) -->
    <MoreSpecificReturnType errorLevel="suppress" />
    <LessSpecificReturnStatement errorLevel="suppress" />

    <!-- Test-specific suppressions -->

    <!-- Redundant test assertions - provide defensive runtime validation -->
    <RedundantCondition>
      <errorLevel type="suppress">
        <directory name="tests" />
      </errorLevel>
    </RedundantCondition>
    <RedundantConditionGivenDocblockType>
      <errorLevel type="suppress">
        <directory name="tests" />
      </errorLevel>
    </RedundantConditionGivenDocblockType>
    <ArgumentTypeCoercion>
      <errorLevel type="suppress">
        <directory name="tests" />
      </errorLevel>
    </ArgumentTypeCoercion>

    <!-- Test validation issues -->
    <InvalidArgument>
      <errorLevel type="suppress">
        <directory name="tests" />
      </errorLevel>
    </InvalidArgument>

    <!-- Test helper methods in anonymous classes -->
    <UndefinedMethod>
      <errorLevel type="suppress">
        <file name="tests/Strategies/AbstractMaskingStrategyTest.php" />
        <file name="tests/Strategies/AbstractMaskingStrategyEnhancedTest.php" />
      </errorLevel>
    </UndefinedMethod>

    <!-- Test function calls -->
    <UndefinedFunction>
      <errorLevel type="suppress">
        <directory name="tests" />
      </errorLevel>
    </UndefinedFunction>

    <!-- Laravel-specific patterns -->
    <!-- (Laravel directory is excluded from scanning) -->

    <!-- Public API surface is intentionally wider than internal usage (161 issues) -->
    <PossiblyUnusedMethod errorLevel="suppress" />

    <!-- Array access patterns for configuration (25 + 1 issues) -->
    <PossiblyUndefinedArrayOffset errorLevel="suppress" />
    <PossiblyInvalidArrayOffset errorLevel="suppress" />

    <!-- Closure patterns in callbacks (88 + 88 + 18 issues) -->
    <UnusedClosureParam errorLevel="suppress" />
    <MissingClosureParamType errorLevel="suppress" />
    <MissingClosureReturnType errorLevel="suppress" />
  </issueHandlers>
</psalm>
